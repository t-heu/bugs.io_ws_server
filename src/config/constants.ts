export const PING_INTERVAL = 30000;  // Intervalo de ping em ms
export const MAX_PING_ATTEMPTS = 3; // Tentativas antes de desconectar
export const INACTIVITY_TIMEOUT = 60000; // 60 segundos
export const RATE_LIMIT_MS = 100;
